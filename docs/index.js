const {
	
  content: [
  	`${themeDir
} = require("./themes/terminal/assets/css/tailwind/tailwind.config.dev");
const {
	
  content: [
  	`${themeDir
} = require("./themes/terminal/assets/css/tailwind/tailwind.config");
module.exports = {
	
  content: [
  	`${themeDir,
	
  content: [
  	`${themeDir
};
